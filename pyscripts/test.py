import json

# Define a dictionary to be converted to JSON
my_dict = {'foo': 'bar', 'baz': [1, 2, 3]}

# Convert the dictionary to a JSON string
json_str = json.dumps(my_dict)

# Print the JSON string to stdout
print("hello")